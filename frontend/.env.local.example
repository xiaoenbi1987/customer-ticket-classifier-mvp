# 本文件是本地开发用的模板：cp .env.local.example .env.local。
# 线上（Vercel）不读这个文件，同名变量配在 Vercel 项目的 Environment Variables 里。

# 后端 FastAPI 服务地址
#
# Vercel 线上取值：https://<你的服务>.up.railway.app （末尾不要带斜杠）
#
# 注意：NEXT_PUBLIC_* 是构建时内联进前端产物的，不是运行时读取的。
# 在 Vercel 改完这个变量必须重新 Deploy 一次才会生效。
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
