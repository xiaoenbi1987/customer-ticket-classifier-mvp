# 本文件是本地开发用的模板：cp .env.example .env 后按需修改。
# 线上（Railway）不读 .env，同名变量直接配在 Railway 的 Variables 里。

# === 数据源开关（必填） ===
# mock: 使用模拟数据（当前阶段）  real: 使用真实历史工单数据（接入后切换）
DATA_SOURCE=mock

# === 可选 ===
# 人工调整记录 SQLite 文件位置，默认在 backend/data/adjustment_log.sqlite3
#
# Railway 上留空的话，数据会随容器一起在每次重新部署时消失。
# 挂了 Volume（挂载点 /data）之后设成 /data/adjustment_log.sqlite3，
# 标注数据就能跨部署保留。
ADJUSTMENT_LOG_DB_PATH=

# 允许跨域访问后端的前端地址，多个用逗号分隔。
# 前端是纯浏览器端请求，这里没配对的话线上会直接被 CORS 挡掉。
#
# Railway 线上取值示例（保留 localhost 是为了本地前端也能连线上后端调试）：
#   CORS_ALLOW_ORIGINS=https://<你的项目>.vercel.app,http://localhost:3000
# 注意 Vercel 的 preview 部署域名每次都不同，不在白名单里，只有正式域名能用。
CORS_ALLOW_ORIGINS=http://localhost:3000
